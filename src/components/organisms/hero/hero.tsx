import { HeroContainer } from '../../molecoles/hero-container/hero-container'
import './style.css'


export const Hero = () => {

    return (
        <section className="hero">
            <HeroContainer/>
        </section>
    )
}